[phases.setup]
nixPkgs = [
  "pkg-config",
  "cairo",
  "pango",
  "libjpeg",
  "giflib",
  "librsvg",
  "pixman",
  "libuuid",
  "python3"
]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "node index.js"
